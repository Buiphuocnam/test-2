<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Câu chuyện về con thỏ và con bướm</title>
    <style>
        /* Căn chỉnh body */
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f9f9f9;
            font-family: Arial, sans-serif;
            box-sizing: border-box;
        }

        /* Tiêu đề */
        h1 {
            font-size: 28px;
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }

        /* Nội dung hình ảnh */
        img {
            width: 60%; /* Đảm bảo hình ảnh chiếm 60% chiều rộng màn hình */
            max-width: 600px; /* Giới hạn tối đa chiều rộng */
            margin-bottom: 40px;
            border-radius: 8px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1);
        }

        /* Video căn giữa */
        video {
            width: 80vw; /* Chiếm 80% chiều rộng màn hình */
            max-width: 1280px; /* Giới hạn chiều rộng tối đa */
            max-height: 720px; /* Giới hạn chiều cao tối đa */
            height: auto;
            border: 2px solid #ccc;
            border-radius: 10px;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
     
